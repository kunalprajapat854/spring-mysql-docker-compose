spring:
  application:
    name: Spring-boot-mysql-docker-compose
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    
    url: jdbc:mysql://mysqldb:3306/practice
    password: root
    username: root
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
server.port: 9090